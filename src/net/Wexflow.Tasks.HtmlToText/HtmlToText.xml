<?xml version="1.0" encoding="utf-8" ?>
<Tasks>
  <!--
    HtmlToText is a sequential task that extracts text from HTML files.
    
    The text files generated are loaded by this task so that other tasks can select them through the selectFiles option.
  -->
  <Task id="$int" name="HtmlToText" description="$string" enabled="true|false">
    <!-- 
      The HTML (.html) files loaded by the task having as id $taskId will be converted to text files.
    -->
    <Setting name="selectFiles" value="$taskId" />
    <Setting name="selectFiles" value="$taskId" />
    <!-- You can add as many selecteFiles as you want.-->

    <!-- Optional. Samba computer name. -->
    <Setting name="smbComputerName" value="$string" />
    <!-- Optional. Samba domain name. -->
    <Setting name="smbDomain" value="$string" />
    <!-- Optional. Samba username. -->
    <Setting name="smbUsername" value="$string" />
    <!-- Optional. Samba password. -->
    <Setting name="smbPassword" value="$string" />
  </Task>
</Tasks>
